# URL du backend FastAPI (Shazam Malgache). L'interface y reverse-proxy /api/*.
# Dev local : http://localhost:8000 (valeur par défaut)
# docker-compose : http://api:8000
# Cette valeur est figée au build de Next (rebuild nécessaire si elle change).
API_URL=http://localhost:8000
